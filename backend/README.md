# FinAlly Backend

FastAPI backend for the FinAlly AI Trading Workstation. Managed with [uv](https://docs.astral.sh/uv/).

## Development

```bash
uv sync
uv run pytest
```

See `planning/PLAN.md` and `planning/MARKET_DATA_DESIGN.md` for the full project and market-data subsystem design.
